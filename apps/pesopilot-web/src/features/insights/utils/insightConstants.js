export const INSIGHT_SCOPES = Object.freeze({
  allData: 'all_data',
  currentCutoff: 'current_cutoff',
  monthly: 'monthly',
  specificCutoff: 'specific_cutoff',
})

export const DEFAULT_INSIGHT_SCOPE = INSIGHT_SCOPES.currentCutoff
