# Public frontend API base URL. Safe to expose in the browser.
VITE_PEOPLEOPS_API_BASE_URL=http://localhost:8080/api/peopleops

# Keep live deployments browseable when the backend is offline.
VITE_DEMO_FALLBACK_ENABLED=true
